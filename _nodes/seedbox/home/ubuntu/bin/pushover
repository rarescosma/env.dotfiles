#!/usr/bin/env bash

set -xe

source "${HOME}/.config/pushover.conf"

curl -sS \
    -F "token=${TOKEN}" \
    -F "user=${USER}" \
    -F "message=${*}" \
    "https://api.pushover.net/1/messages.json"

