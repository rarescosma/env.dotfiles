#!/usr/bin/env bash

REPO="${1}"

wget -q "https://registry.hub.docker.com/v1/repositories/${1}/tags" -O - \
  | sed -e 's/[][]//g' -e 's/"//g' -e 's/ //g' \
  | tr '}' '\n'  \
  | awk -F: '{print $3}'
