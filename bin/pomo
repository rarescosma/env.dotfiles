#!/usr/bin/env bash

pomo_time="${1:-1200}"
grace_time="${2:-30}"

sleep "$pomo_time"
notify-send "Time for a break. Locking in ${grace_time}s ..."
sleep "$grace_time"
playerctl -a pause
${HOME}/.i3/locker.sh
