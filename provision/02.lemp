if [ <%- provision.lemp %> = true ] ; then

	# Nginx
	sai nginx

	# MariaDB
	sudo apt-key adv --recv-keys --keyserver hkp://keyserver.ubuntu.com:80 0xcbcb082a1bb943db
	sudo add-apt-repository 'deb http://mirrors.linsrv.net/mariadb/repo/10.0/ubuntu trusty main'
	sau
	sai mariadb-server

	# PHP
	sai php5-fpm
	sai <%- lemp.php_extensions %>

fi
